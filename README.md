# into-code-

A small CLI project that asks for a class name and attributes, then generates runnable Java code.

## Usage

1. Run the generator:

```bash
npm start
```

2. Enter:
   - class name
   - number of attributes
   - each attribute name and type

3. The app creates `<ClassName>.java` in the current directory.

## Test

```bash
npm test
```
